# Visual QA - 2026-08-09

Scope: Zielmodus 4 dark-mode QA for Knowledge Browser and Knowledge Graph.

## Environment

- Local source: `frontend/`
- Local preview: `http://127.0.0.1:8766/`
- Tested views:
  - `?view=concepts`
  - `?view=graph`
- Tested widths:
  - desktop: requested `1440x1000`, browser reported `1600px` viewport width
  - mobile: requested `390x900`, browser reported `433px` viewport width

## Findings

### Knowledge Browser

Status: pass.

- Desktop: no out-of-bounds elements detected.
- Mobile: no out-of-bounds elements detected.
- Mobile shell now collapses to one column.
- Sidebar no longer consumes the left side of the mobile viewport.
- Concept cards stay inside the content column.
- Dark-mode cards retain readable contrast.

Measured mobile containment:

| Selector | Left | Right | Width | Result |
|---|---:|---:|---:|---|
| `.shell` | 0 | 417 | 417 | pass |
| `.sidebar` | 0 | 417 | 417 | pass |
| `.main` | 0 | 417 | 417 | pass |
| `#concepts.view.active` | 22 | 395 | 373 | pass |
| `.knowledge-increment` | 40 | 376 | 336 | pass |

### Knowledge Graph

Status: pass.

- Desktop: no out-of-bounds elements detected.
- Mobile: no out-of-bounds elements detected after constraining `.graph-cockpit-panel` to `minmax(0, 1fr)`.
- Graph canvas remains internally scrollable where needed.
- Graph header, command surface, canvas wrapper, and detail panel are contained inside the mobile viewport.
- Approved/draft/inferred graph language remains visually available through the existing legend and node styling.

Measured mobile containment:

| Selector | Left | Right | Width | Result |
|---|---:|---:|---:|---|
| `.shell` | 0 | 417 | 417 | pass |
| `.sidebar` | 0 | 417 | 417 | pass |
| `.main` | 0 | 417 | 417 | pass |
| `#graph.view.active` | 22 | 395 | 373 | pass |
| `.graph-cockpit-head` | 40 | 376 | 336 | pass |
| `.graph-command-surface` | 40 | 376 | 336 | pass |
| `.graph-canvas-wrap` | 40 | 376 | 336 | pass, internal scroll |
| `.graph-detail-panel` | 40 | 376 | 336 | pass |

## Fix Applied

File: `frontend/styles.css`

- Added a final mobile generated-layer guard after the latest MeIDs cockpit override block.
- Constrained mobile `.shell` to a single column.
- Hid sidebar panels on mobile and converted navigation to a horizontal rail.
- Constrained `.knowledge-browser-panel` and `.graph-cockpit-panel` to `grid-template-columns: minmax(0, 1fr)`.
- Constrained graph and knowledge child panels with `width: 100%`, `max-width: 100%`, and `min-width: 0`.

## Validation

Commands:

```powershell
node scripts/pages-smoke-check.cjs frontend
node --check frontend/app.js
node scripts/validate-n8n-fixtures.cjs
node scripts/validate-okf-fixtures.cjs
node scripts/validate-graph-promotions.cjs
```

Results:

- Pages smoke check passed.
- Frontend JavaScript syntax check passed.
- n8n fixture validation passed: 3 fixtures, 12 replay cases.
- OKF fixture validation passed: 15 files across 14 fixture groups.
- Graph promotion validation passed: 3 promotion fixtures against 2 edge fixtures.

Browser screenshot capture through the control layer timed out during this pass. DOM bounds and page overflow measurements were captured successfully and are the QA evidence for this iteration.

## Remaining Open Items

- Real n8n URLs are still missing for:
  - Knowledge Fabric Agent
  - Agentic Butler
- Live n8n contract status can only move from fixture/config readiness to connected after those URLs are configured.
- Azure/vector DB credentials are still intentionally out of scope.
