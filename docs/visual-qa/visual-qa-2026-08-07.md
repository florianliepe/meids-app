# Visual QA Report - 2026-08-07

Scope: GitHub Pages static frontend artifact for Chat / Actor Twin workspace, Knowledge Browser, and Knowledge Graph.

Environment:

- Local static artifact: `dist-pages-local-20260807-qa06`
- Browser: Google Chrome via Playwright
- Desktop viewport: `1440 x 1000`
- Mobile viewport: `390 x 900`
- Theme: dark mode forced during QA

## Findings

| Area | Finding | Action |
| --- | --- | --- |
| Static asset loading | Static artifact on localhost was routed to `/static/...`, causing raw unstyled HTML outside the FastAPI MVP server. | Narrowed backend asset mode to known MVP ports `8501` and `8601`; static artifacts now use relative assets. |
| Mobile shell | A late `.shell` override re-expanded the sidebar/main layout on mobile. | Added final mobile overrides to restore single-column layout and prevent page-level horizontal overflow. |
| Chat | Desktop and mobile route correctly, no page-level overflow, no clipped buttons. | No further code change needed. |
| Knowledge Browser | Desktop and mobile route correctly with public static fallback data. Dense lists remain scrollable/contained. | Added mobile containment for health rows, review workspace, and cockpit chip rows. |
| Knowledge Graph | Graph route renders SVG topology nodes and relation lines. Dense graph canvas scrolls internally on mobile. | Added graph containment and mobile commandbar stacking. |

## Compact QA Result

| Case | Route | Page overflow | App asset failures | Render evidence |
| --- | --- | ---: | ---: | --- |
| Chat desktop | `?view=chat` | `0` | `0` | Chat workspace rendered |
| Knowledge desktop | `?view=concepts` | `0` | `0` | 3 public-safe concept cards rendered |
| Graph desktop | `?view=graph` | `0` | `0` | 17 graph SVG nodes and 5 relation labels/edges counted |
| Chat mobile | `?view=chat` | `0` | `0` | Chat workspace rendered |
| Knowledge mobile | `?view=concepts` | `0` | `0` | 3 public-safe concept cards rendered |
| Graph mobile | `?view=graph` | `0` | `0` | 17 graph SVG nodes and 5 relation labels/edges counted |

Known residual: the graph canvas intentionally has internal horizontal scroll on small mobile screens because topology readability is better preserved than compressing all nodes into an unreadable column.
